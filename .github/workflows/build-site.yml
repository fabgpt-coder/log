name: build-site

on:
  schedule:
    - cron: '17 5 * * *'   # daily at 05:17 UTC
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/build_data.py'
      - 'docs/**'
      - '!docs/index.md'        # auto-generated, don't trigger on its own change
      - '!docs/public/prs.json' # auto-generated
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/build-site.yml'

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: build-site
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      commit_sha: ${{ steps.commit.outputs.sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - run: pip install --quiet pyyaml==6.*

      # If data generation fails (network / rate-limit / API outage), keep going:
      # VitePress will rebuild using the previously-committed prs.json, so the
      # site stays live with stale-but-valid data rather than turning red.
      - name: Generate data + README + homepage
        id: gen
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          AGENT_GH_LOGIN: fabgpt-coder
          LOG_REPO: ${{ github.repository }}
          SITE_URL: https://fabgpt-coder.github.io/log/
        run: python3 scripts/build_data.py

      - name: Warn on generation failure
        if: steps.gen.outcome == 'failure'
        run: echo "::warning::build_data.py failed — rebuilding site with previous prs.json"

      - name: Commit generated files if changed
        id: commit
        if: steps.gen.outcome == 'success'
        run: |
          set -euo pipefail
          git config user.name "fabgpt-coder[bot]"
          git config user.email "fabgpt-coder@users.noreply.github.com"
          git add README.md docs/index.md docs/public/prs.json
          if git diff --cached --quiet; then
            echo "no changes"
            echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git commit -m "auto: daily rebuild"
          git push
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci --no-audit --no-fund

      - name: Build VitePress
        run: npm run docs:build
        env:
          NODE_ENV: production

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs/.vitepress/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
